# env_ur — Robot UR16e (ur-rtde)
# Régénérer : conda_envs/export_all.ps1 sur Windows
name: env_ur
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - cmake
  - pip
  - pip:
      - ur-rtde
